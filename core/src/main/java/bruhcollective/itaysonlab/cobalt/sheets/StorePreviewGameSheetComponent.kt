package bruhcollective.itaysonlab.cobalt.sheets

/**
 * A game sheet for all other games (a store preview). That means everything clicked outside of Library - even if owned.
 *
 * Has wishlist, game information, store button.
 */
interface StorePreviewGameSheetComponent {
}